# Sample Reports Directory

Place your medical report PDF files here for processing.

## Supported Formats:
- Echocardiography reports
- Text-based PDFs (not scanned images)
- Reports with M-Mode/2D measurements
- Reports with Doppler measurements

## Example Usage:

```bash
# Process all PDFs in this directory
python ../main.py batch . -o ../data/processed/
```
